import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    background: {
      default: 'black',
    },
    primary: {
        main:'',
        dark:'',
        light:''
    },
    secondary: { 
        main:'',
        dark:'',
        light:'' 
    },
  },
});

export default Theme;